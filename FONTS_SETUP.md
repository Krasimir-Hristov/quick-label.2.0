# 🔤 Настройка на Custom Шрифтове

## 📁 Къде да поставите файловете

Поставете двата шрифтови файла в папката `public/fonts/`:

```
public/
└── fonts/
    ├── Lexia_Std-.ttf
    └── DINEngschriftStd-.otf
```

⚠️ **ВАЖНО**: Имената на файловете трябва да са точно както са конфигурирани:

- `Lexia_Std-.ttf`
- `DINEngschriftStd-.otf`

## 🛠️ Конфигурация (Готова)

Всички конфигурации са готови:

✅ `lib/fonts.ts` - дефинирани шрифтовете  
✅ `app/layout.tsx` - интегрирани в приложението  
✅ `app/globals.css` - добавени CSS класове

## 🎨 Как да използваме шрифтовете

### В React компоненти:

```tsx
// Чрез CSS класове
<div className="font-lexia-std">Текст с Lexia Std шрифт</div>
<div className="font-din-engschrift-std">Текст с DIN Engschrift Std шрифт</div>

// Чрез inline стилове
<div style={{ fontFamily: 'var(--font-lexia-std)' }}>Lexia шрифт</div>
<div style={{ fontFamily: 'var(--font-din-engschrift-std)' }}>DIN шрифт</div>
```

### В CSS файлове:

```css
.my-element {
  font-family: var(--font-lexia-std), sans-serif;
}

.another-element {
  font-family: var(--font-din-engschrift-std), sans-serif;
}
```

## 🏷️ Прилагане в етикетите

За да използваме шрифтовете в етикетите, просто добавете CSS класа или стил:

```tsx
// В Label.tsx
<div className='font-lexia-std' style={{ fontSize: '24.8pt' }}>
  APP VORTEIL
</div>
```

## 🔧 Отстраняване на проблеми

Ако шрифтовете не се зареждат:

1. ✅ Проверете дали файловете са в `public/fonts/`
2. ✅ Проверете дали имената са точни
3. ✅ Рестартирайте dev сървъра (`npm run dev`)
4. ✅ Проверете браузърския инспектор за грешки

## 📋 Checklist

- [ ] Файлът `Lexia_Std-.ttf` е в `public/fonts/`
- [ ] Файлът `DINEngschriftStd-.otf` е в `public/fonts/`
- [ ] Рестартиран е dev сървърът
- [ ] Шрифтовете се появяват в браузъра
